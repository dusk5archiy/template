# VcXsrv

## Server (Ubuntu)

```bash
export DISPLAY=192.168.137.1:0 ubuntu-frame & chromium
```

## Client (Windows)

- Install `VcXsrv`.
- Run `Xlaunch`.
- Choose `Multiple windows`, display number `0`.
- Click `Next`.
- Click `Next`.
- Check `Disable access control`.
- Click `Finish`.
